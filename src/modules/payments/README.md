# Payments Module

Capas del modulo:

- `domain`: tipos de recarga, estados de formulario y constantes.
- `application`: servicios de recargas + server actions para usuario/admin.
- `infrastructure`: repositorio Supabase (RPCs, tablas y storage de comprobantes).
- `ui`: formularios de recarga y tablas para usuario y administrador.
